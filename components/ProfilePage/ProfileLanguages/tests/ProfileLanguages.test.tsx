import { useAuth } from '@/contexts/AuthContext';
import '@testing-library/jest-dom';
import {
  findAllByTestId,
  findByTestId,
  fireEvent,
  render,
  waitFor,
} from '@testing-library/react';
import ProfileLanguages from '../ProfileLanguages';

it('IS NOT A REAL TEST', async () => {
  expect(false);
});
