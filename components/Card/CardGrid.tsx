const CardGrid = ({
  children,
  gridTemplateColumns,
  gridTemplateRows,
}: {
  children: React.ReactNode;
  gridTemplateColumns: string;
  gridTemplateRows: string;
}) => {
  return <div></div>;
};

export default CardGrid;
